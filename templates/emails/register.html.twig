<main>
    <h1>Activation de compte sur Depixerlizer</h1>
    <h2>Bonjour {{ user.username }}</h2>
    <p>Vuillez suivre ce lien pour valider la création de votre compte et accéder à tous vos privilèges</p>
    <p>
        <a href="{{ absolute_url(path('app_verify_user', {token: token})) }}">Lien</a>
    </p>
    {{ token }}
    <p>Merci !</p>
</main>